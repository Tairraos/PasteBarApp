# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
local.pastebar.*
safelist.txt
pastebar-data-backup*

clipboard-images/**/*
clip-images/**/*
src-tauri/http-cacache/**/*
pastebar_settings.yaml

node_modules
packages/dist-ui/**/*
dist-ssr
build/**/*
*.local
*.data

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?


# Added by cargo

/target

# --- harness: never track machine-local config or generated build output -------------
# `.env` is machine-local (see .env.sample). Copy it, never commit it.
.env
.env.local
.env.*.local

# Vite writes this temp file next to the config while loading it.
vite.config.mts.timestamp-*.mjs
*.timestamp-*.mjs

# Generated by scripts/twSafelistGenerator — regenerate with `npm run build`.
tailwind-safelist.txt

# Translation audit output (written into the CWD by scripts/translation-audit.ts).
translation-audit-report.txt

# Vitest / coverage output
coverage/
